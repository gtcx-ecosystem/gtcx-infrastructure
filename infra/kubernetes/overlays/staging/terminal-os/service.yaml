# =============================================================================
# terminal-os — Staging Service
# =============================================================================
apiVersion: v1
kind: Service
metadata:
  name: terminal-os
  namespace: terminal-os-staging
  labels:
    app: terminal-os
    tier: frontend
    environment: staging
  annotations:
    alb.ingress.kubernetes.io/healthcheck-path: /api/health
    alb.ingress.kubernetes.io/success-codes: '200'
spec:
  type: ClusterIP
  selector:
    app: terminal-os
    environment: staging
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
