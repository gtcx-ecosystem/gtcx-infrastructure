# =============================================================================
# Secret Scanning — TruffleHog
# =============================================================================
# Scans repository for leaked secrets, API keys, tokens, and credentials.
# Fails CI if any verified secrets are found.
#
# Tools: TruffleHog (trufflesecurity/trufflehog)
# Coverage: Entire git history + current working tree
# =============================================================================

name: Secret Scan

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  trufflehog:
    name: TruffleHog Secret Scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          fetch-depth: 0

      - name: Run TruffleHog
        uses: trufflesecurity/trufflehog@5a1d434eea627c09bba83124adc8a7246d8183e8 # pin: main (branch — review on dependabot bumps)
        with:
          path: ./
          base: ${{ github.event.repository.default_branch }}
          head: HEAD
          extra_args: --only-verified
