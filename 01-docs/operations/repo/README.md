# Repo hygiene — operations

Machine-readable policy for workspace root cleanliness (repo-hygiene protocol).

| File                         | Role                  |
| ---------------------------- | --------------------- |
| `repo-hygiene-protocol.md`   | Normative rules       |
| `root-allowlist.json`        | Allowed root paths    |
| `root-allowlist.schema.json` | Allowlist JSON schema |

Gate: `pnpm check:workspace-root-cleanliness:strict`
