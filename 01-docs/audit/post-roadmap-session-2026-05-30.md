---
title: 'Post-Roadmap Session Audit + Strategic Plan'
status: 'current'
date: '2026-05-30'
owner: 'gtcx-infrastructure'
tier: 'critical'
tags: ['audit', 'strategy', 'post-mortem', 'dependabot', 'roadmap']
review_cycle: 'on-change'
provenance: 'Generated by Claude Opus 4.7 (1M context) — Claude-internal audit; NOT an external/independent attestation. See "Provenance + meta-caveat" section.'
---

# Post-Roadmap Session — Audit + Triage + Strategic Plan

**Session date:** 2026-05-30
**Baseline:** `5b8d323e` (prior internal 10/10 sign-off, 2026-05-28)
**HEAD:** `d0b00b1` (post 33-commit roadmap session)
**Independent scoring:** 6.8/10 internal · 6.2/10 certified
**Prior session's self-claim:** 8.5/10 internal · 7.5/10 certified

This document captures three pieces of work that lived only in the
conversation transcript and would otherwise be lost: (1) a fresh
independent audit of the 33-commit roadmap session, (2) per-PR
triage of the 16 open dependabot PRs as of HEAD, and (3) the
strategic plan derived from both. It is committed so the next agent
or contributor can pick up without rebuilding from `git log`.

---

## Provenance + meta-caveat

This audit was conducted by Claude (the same agent that shipped the
33-commit roadmap session being audited) using fresh-context
subagents with no prior-session memory. Two cycles of internal
auditing have now produced the same failure mode: the team that
ships the work cannot reliably audit its own work, even with
delegate-to-fresh-agent patterns. The single most important finding
of this audit is that **external review is now required** — see
[Strategic Bet 1](#bet-1--close-the-technical-bug-debt--execute-the-pen-test-4-weeks).

A pen-test vendor or buyer-side reviewer would close this gap. This
internal document does NOT.

---

## Part 1 — Audit findings

### Scoring delta vs prior session

| Dimension           | Prior session's self-claim | Independent re-score | Delta |
| ------------------- | -------------------------- | -------------------- | ----- |
| Internal readiness  | 8.5/10                     | **6.8/10**           | -1.7  |
| Certified composite | 7.5/10                     | **6.2/10**           | -1.3  |

The session's net effect was **negative**: it closed 5 prior P0s
(F1-F5 from the 2026-05-28 audit) but introduced 4 new P0s and 11
new P1s. Four of the seven new `pnpm test` gates I added had
bypasses (same regex-bug class as the S3.4 bug that originally
prompted this exact warning). Two new helper primitives (`failClosed`,
`budget-store`) have zero callers in production code — they were
shipped as "fixes" that enforce nothing.

### Did the prior P0/P1 fixes hold?

| Prior finding                        | Verdict                                                                      |
| ------------------------------------ | ---------------------------------------------------------------------------- |
| F1 — Cross-tenant evidence leak      | HELD — strict tenantId filter via recordTenants sidecar                      |
| F2 — audit-query bearer pass-through | HELD on `/audit/query`; same class bug newly found on `/audit/bundles`       |
| F3 — audit-flush S3 silent stub      | HELD; stub path still mutates `lastSuccessMs` (staleness signal compromised) |
| F4 — Replay-guard fail-open          | HELD on construction-time check; new exempt-path traversal bypass introduced |
| F5 — PagerDuty env vars              | HELD via envsubst init container; `CHANGE_ME_DEV` default leaks if env unset |

5/5 held in the literal sense. 4/5 shipped a related new bug.

### Consolidated finding registry (27 findings)

| #   | Finding                                                                                                          | Severity | Source file:line                                                                       |
| --- | ---------------------------------------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------- |
| F1  | `pnpm test` RED on main — `01-docs/roadmap/ROADMAP-2026-07-13.md` filename + missing roadmap README              | P0       | `01-docs/roadmap/`                                                                     |
| F2  | Replay-guard `/_next/../v1/query` exempt-path traversal — bypasses ALL replay protection                         | P0       | `03-platform/tools/replay-protection/src/middleware.mjs:130-139`                       |
| F3  | `/audit/bundles` `X-GTCX-Tenant-Id` header pollution — DID-auth devices stamp arbitrary tenants                  | P0       | `03-platform/tools/compliance-gateway/src/audit-bundles/handler.mjs:73,132`            |
| F4  | `/v1/exceptions` blind to auth-failure events — `payload.tenantId='unknown'` never matches tenant filter         | P0       | `03-platform/tools/compliance-gateway/src/server.mjs:236` + `audit.mjs:444`            |
| F5  | `failClosed.mjs` defined + tested + **imported nowhere** — silent-failure pattern unchanged                      | P1       | `03-platform/tools/scripts/fail-closed.mjs`                                            |
| F6  | `budget-store.mjs` defined + tested + unwired — same theatrical pattern                                          | P1       | `03-platform/tools/compliance-gateway/src/budget-store.mjs`                            |
| F7  | 31 of 44 alert `runbook_url` anchors are dead in `alerts.md`                                                     | P1       | `01-docs/04-ops/runbooks/alerts.md` vs `04-ship/monitoring/alerts/*.yml`               |
| F8  | `auth-failure-throttle` ipState Map unbounded — OOM under distributed brute-force                                | P1       | `03-platform/tools/compliance-gateway/src/auth-failure-throttle.mjs:26`                |
| F9  | `sourceIpFromRequest` trusts XFF first hop unconditionally — throttle bypass via spoofing                        | P1       | `03-platform/tools/compliance-gateway/src/auth-failure-throttle.mjs:107-113`           |
| F10 | Catalog `verify-catalog.mjs` has no pinned trust anchor — self-vouching                                          | P1       | `03-platform/tools/compliance-data/03-platform/scripts/verify-catalog.mjs:60-64`       |
| F11 | `pin-actions-sha` regex misses `uses : ...` (space) and `uses:...` (no space) — 2 new bypasses                   | P1       | `03-platform/tools/scripts/pin-actions-sha.mjs:34`                                     |
| F12 | `empty-catch-check` misses destructured + comment-only bodies                                                    | P1       | `03-platform/tools/scripts/empty-catch-check.mjs:46`                                   |
| F13 | `runbook-commands-check` bypassed by env-var prefix (`FOO=1 pnpm ...`)                                           | P1       | `03-platform/tools/scripts/runbook-commands-check.mjs:96`                              |
| F14 | `dependabot-triage` workflow references labels that don't exist → silent 422                                     | P1       | `.github/workflows/dependabot-triage.yml:43-49`                                        |
| F15 | `/v1/exceptions` + `/v1/audit/evidence-bundle` have zero Prometheus metrics                                      | P1       | `03-platform/tools/compliance-gateway/src/server.mjs`                                  |
| F16 | `runbook-frontmatter-check` merge degraded `automated-rollback.md` from `tier: critical` → `tier: informational` | P2       | `03-platform/tools/scripts/runbook-frontmatter-check.mjs:114`                          |
| F17 | HTML evidence renderer no CSP header + no Unicode RTL handling                                                   | P2       | `03-platform/tools/compliance-gateway/src/evidence-renderer.mjs:87` + `server.mjs:793` |
| F18 | KYC predictable verdict via known default salt + idempotency docstring lies                                      | P2       | `03-platform/tools/kyc-screening/03-platform/src/handler.mjs:46-47,101-103`            |
| F19 | `auth-failure-throttle` check-then-record race — same-IP burst over-shoots threshold                             | P2       | `03-platform/tools/compliance-gateway/src/server.mjs:204-222`                          |
| F20 | Stub S3 mutates `lastSuccessMs` — staleness signal compromised                                                   | P2       | `03-platform/tools/audit-flush/src/s3-uploader.mjs:83`                                 |
| F21 | CHANGE_ME_DEV default in `docker-compose.infra.yml` leaks to prod if env unset                                   | P2       | `04-ship/docker/docker-compose.infra.yml:151`                                          |
| F22 | Node pin not enforced: 9 packages `>=20.0.0`, all 7 CI workflows bare `'20'`                                     | P2       | workspace `package.json` files + `.github/workflows/*.yml`                             |
| F23 | 16 dependabot PRs unlabeled, oldest 21 days                                                                      | P2       | `gh pr list --state open`                                                              |
| F24 | No quarterly DR-drill artifact in `04-ship/security/reports/`                                                    | P2       | `04-ship/security/reports/`                                                            |
| F25 | No committed soak-test baseline — regressions can't fail build                                                   | P2       | `03-platform/tools/load-tests/`                                                        |
| F26 | `compliance-data` canonicalize divergence risk vs `@gtcx/audit-signer`                                           | P3       | `03-platform/tools/compliance-data/03-platform/scripts/sign-catalog.mjs:51-61`         |
| F27 | KYC handler key validation missing — accepts `\n` in keys                                                        | P3       | `03-platform/tools/kyc-screening/03-platform/src/handler.mjs:123`                      |

**Totals:** 4 P0 · 11 P1 · 9 P2 · 3 P3

### Gates that don't enforce what they claim

| Gate                            | Bypass discovered                                                                                              |
| ------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| `pin-actions-sha.mjs`           | `uses : actions/checkout@v4` (space before colon) and `uses:actions/...` (no space after colon) both slip past |
| `empty-catch-check.mjs`         | `catch ({ x }) {}` (destructured), `catch (err) { /* comment */ }` (comment-only body) both pass               |
| `runbook-commands-check.mjs`    | `FOO=1 pnpm bogus` (env-var prefix) passes                                                                     |
| `alerts-add-runbook-url.mjs`    | Confirms 44 alerts annotated; never validates the URL anchor target exists (31/44 are dead)                    |
| `runbook-frontmatter-check.mjs` | No regex bypass, but merge semantics silently degraded curated `tier:` values                                  |
| `dependabot-triage.yml`         | References labels that don't exist in the repo; `gh pr edit --add-label` silently 422s                         |
| `fail-closed.mjs`               | Architectural: defined + tested + **zero production callers**                                                  |
| `production-overlay-guard.mjs`  | None found — this one's tight                                                                                  |

### Sprint plan derived from the findings (3 weeks)

#### Sprint 1 — Close what this session introduced (1 week)

8 tasks, all remediation. Sample:

1. **(5 min)** Rename `01-docs/roadmap/ROADMAP-2026-07-13.md` → lowercase; add `01-docs/roadmap/README.md` → CI green
2. Replay-guard `path.posix.normalize` + `..`-reject + regression test
3. `/audit/bundles` derive tenantId from resolved DID context, not header
4. Tag auth-failure events with synthetic platform tenant; extend `/v1/exceptions` to mix platform + tenant
5. Wire `failClosed` into audit-flush S3 path + audit-sink NATS publish
6. Fix `pin-actions-sha`, `empty-catch-check`, `runbook-commands-check` regexes
7. Extend `alerts-add-runbook-url` to validate URL anchors; fill 31 missing sections OR drop unready alerts
8. Create `deps:patch/minor/major` + ecosystem labels in repo

**DoD:** `pnpm test` green; every gate has an adversarial test that would catch its prior bypass; every "look fixed but isn't" item resolved.

#### Sprint 2 — Hardening + un-shipping dead code (1 week)

1. `auth-failure-throttle` LRU cap + atomic recordAndCheck + Redis backend
2. Trusted-XFF CIDR enforcement
3. Wire `budget-store` — async migration of `checkBudget/recordSpend/getSpend`
4. Pin `EXPECTED_PUBLIC_KEY` in `verify-catalog.mjs` (20-line moat-unlocking fix)
5. Add Prometheus counters for `/v1/exceptions` + `/v1/audit/evidence-bundle`
6. CSP header + RTL stripping in HTML evidence
7. KYC: idempotency check + key validation + fail-closed on default salt
8. Node 20.18.0 enforcement across all 9 packages + 7 CI workflows

#### Sprint 3 — Operational + pilot prerequisites (1 week)

1. Triage + merge dependabot backlog per the per-PR plan below
2. First quarterly DR drill on testnet RDS; evidence committed
3. Committed soak baseline as PR gate (fail on >25% regression)
4. Replace custom canonicalize with `@gtcx/audit-signer` export
5. Frontmatter-merge fix: refuse merge on `tier:` conflict
6. Wire multi-tenant evidence-bundle endpoint with authorization predicate
7. SLA publication-checklist items 1-3

---

## Part 2 — Dependabot triage (16 open PRs)

**Critical pre-blocker:** every PR fails the same 8 checks because
`main` itself is red (F1). 5-minute fix unblocks all 16.

### Tier 1 — safe to merge on green CI

| PR  | Bump                                   | Action                   |
| --- | -------------------------------------- | ------------------------ |
| #71 | tinyexec 1.2.2 → 1.2.3 (patch, devDep) | Merge first as CI canary |

### Tier 2 — minor

| PR  | Bump                         | Action          |
| --- | ---------------------------- | --------------- |
| #44 | `@eslint/js` 9.28.0 → 9.39.4 | Merge after #71 |

### Tier 3 — CI group bumps (review-then-merge)

| PR  | Bumps inside                                                                                                    | Risk                            |
| --- | --------------------------------------------------------------------------------------------------------------- | ------------------------------- |
| #74 | codeql 3→**4** + sigstore + zaproxy + slsa-verifier                                                             | CodeQL 4 breaking-bundle change |
| #68 | checkout 4→**6**, upload-artifact 4→**7**, download-artifact 4→**8**, setup-node 4→**6**, github-script 7→**9** | upload-artifact v5+ API changes |
| #33 | linting group (21 days stale)                                                                                   | Needs `@dependabot rebase`      |
| #36 | pnpm/action-setup 4 → 6                                                                                         | Used in 8 workflows             |
| #70 | aws-actions/configure-aws-credentials 4 → 6                                                                     | Security-sensitive (OIDC)       |
| #75 | azure/setup-kubectl 4 → 5                                                                                       | Low traffic                     |

### Tier 4 — hot-path majors (BATCH together; do not merge separately)

| PR  | Bump                                |
| --- | ----------------------------------- |
| #69 | `ai` 5.0.192 → 6.0.193              |
| #76 | `@ai-sdk/google` 1.2.22 → 3.0.80    |
| #73 | `@ai-sdk/openai` 1.3.24 → 3.0.67    |
| #72 | `@ai-sdk/anthropic` 1.2.12 → 3.0.81 |

**Pre-merge required:** read `ai` v5→v6 migration guide; verify
`providers.mjs::createModel` still matches; run eval-pipeline +
integration test against each provider tier. CVE-2026-8769 risk
acceptance may close if these versions update.

### Tier 5 — toolchain majors

| PR      | Bump                             | Action                                                                                          |
| ------- | -------------------------------- | ----------------------------------------------------------------------------------------------- |
| #40     | typescript 5.9.3 → 6.0.3         | Run `pnpm typecheck` on PR branch first                                                         |
| #67     | lint-staged 16.4.0 → 17.0.6      | Read changelog; verify config still parses                                                      |
| **#62** | **@types/node 22.19.8 → 25.9.1** | **REJECT** — would expose APIs unavailable on Node 20 runtime. Pin to `~22.x` in dependabot.yml |

### Tier 6 — manual review

| PR  | Detail                                                              |
| --- | ------------------------------------------------------------------- |
| #57 | docs: protocol 1 v2.0 — 4351 line addition; out of dependabot scope |

### Permanent fix to prevent regrowth

Add to `.github/dependabot.yml`:

```yaml
ignore:
  - dependency-name: '@types/node'
    versions: ['23.x', '24.x', '25.x']
  - dependency-name: '@ai-sdk/*'
    update-types: ['version-update:semver-major']
  - dependency-name: 'ai'
    update-types: ['version-update:semver-major']
```

---

## Part 3 — Strategic plan (30-90 day)

### Meta-finding: audit independence is the #1 unblock

Two cycles of internal audit. Same diagnosis: team that ships
cannot reliably audit its own work. Options ranked by leverage:

| Approach                          | Cost                 | Time            | Value                                                              |
| --------------------------------- | -------------------- | --------------- | ------------------------------------------------------------------ |
| **Pen-test vendor (recommended)** | $40-60k              | 4-6 weeks       | External attestation; closes EXT-INF-002; validates internal gates |
| Single security-focused hire      | $200k/yr             | 8-12 weeks ramp | Continuous review; expensive long-term                             |
| Bug bounty                        | $5-20k/mo + bounties | 2 weeks         | Cheap; requires triage capacity                                    |
| Audit-trade with peer             | $0 + reciprocal time | 1-2 weeks       | Free; quality variable                                             |
| Status quo                        | $0                   | Indefinite      | Two cycles confirm doesn't work                                    |

### Product-shape decision (defer to Q3 2026)

The repo houses two products on different cadences:

| Product A: Service                                                | Product B: Primitives                                                        |
| ----------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| compliance-gateway, audit-flush, kyc-screening, mcp, k8s overlays | `@gtcx/audit-signer`, `@gtcx/compliance-data`, `terraform-aws-compliance-db` |
| Sells to CISO / Head of Compliance                                | Sells to developers / staff engineers                                        |
| Roadmap is operational                                            | Roadmap is API + signing trust                                               |

Defer the split. Trigger to revisit: first regulator citation of
the signed catalog (S6.1).

### ZWCMP — what actually blocks signature

The technical blockers (Sprint 1 P0s) are real but not the whole
story. Honest inventory:

| Category         | Blockers                                                                                                      |
| ---------------- | ------------------------------------------------------------------------------------------------------------- |
| Technical        | Audit Sprint 1 P0s; tenant-onboarding cluster rehearsal                                                       |
| Operational      | Real-AWS DR drill; on-call rotation; first WORM write; ALB health proof                                       |
| Legal/Commercial | Pilot agreement signature; DPA ZW↔af-south-1; SLA legal review + insurance + cap; SLA referenced in agreement |
| Customer-trust   | First call from ZWCMP-side compliance lead; demo with locally-verified evidence bundle; named GTCX owner      |
| GTCX-internal    | `pilot-success-criteria.md:38` "GTCX owner" line still empty                                                  |

**The question that matters this week:** _Who at ZWCMP last spoke to whom at GTCX, when, and what was the result?_ If "no one in 30+ days," technical work alone won't close the pilot.

### Three 90-day strategic bets

#### Bet 1 — Close the technical bug debt + execute the pen-test (4 weeks)

- Audit Sprint 1 + Sprint 2 (close 4 P0s, fix 4 gate-bypasses, wire dead primitives, pin catalog trust anchor)
- Engage pen-test vendor; scope = compliance-gateway + audit chain + IAM
- **Gate:** pen-test report in hand; all P0s closed; `failClosed` + `budget-store` have ≥3 production callers; catalog verifier rejects key-swap attack

**Cost:** 1-2 engineers × 4 weeks + $40-60k vendor

#### Bet 2 — Sign ZWCMP (parallel, 6-8 weeks)

- Assign named GTCX owner (CEO or VP eng)
- Weekly cadence call with ZWCMP technical + compliance leads
- Real DR drill on testnet RDS with evidence committed
- Hand SLA draft to legal; get insurance quote; fill TBD numbers
- Pilot agreement signed referencing SLA v1.0

**Gate:** signed pilot + first cooperative onboarded + first signed evidence-bundle delivered

**Cost:** 0.25-0.5 FTE eng + 0.5 FTE non-eng + ~$10-20k legal + insurance

#### Bet 3 — Publish primitives for real (parallel, 4 weeks)

- Flip `private: true` → false on `@gtcx/audit-signer` + `@gtcx/compliance-data`
- npm publish with provenance (slsa-provenance workflow already exists)
- First external blog post + outreach to 3-5 African fintech/regtech engineers
- Pin catalog public key in verifier (the 20-line moat-unlocking fix)

**Gate:** packages live on npm with ≥1 external citation; verifier accepts only pinned key

**Cost:** 0.25 FTE eng + 0.25 FTE devrel/marketing

### What this plan deliberately doesn't do

- **No new features.** Sprint 6's moat bets gave the substrate
  shape; further code without external validation is more dead-
  `failClosed`-class bets.
- **No SOC 2 pursuit yet.** 6-12 month engagement; pays off at
  enterprise scale; ZWCMP doesn't need it. Defer to Q4 2026.
- **No new jurisdictions** in the catalog. Quality > quantity.
- **No sales hire yet.** Founder-led for first 3 pilots; hire
  after the third signature when motion is repeatable.

### Decision tree

```
Product-led or sales-led for next 12 months?
├── product-led: Bet 3 is priority; ZWCMP nice-to-have; hire devrel
└── sales-led:  Bet 2 is priority; Bet 3 nice-to-have; hire CSM after pilot #2

Either way: Bet 1 (technical debt + pen-test) is mandatory.
```

### Recommended 4-week sequence

| Week | Bet 1 (tech debt + pen-test)        | Bet 2 (ZWCMP)                                        | Bet 3 (primitives)                                |
| ---- | ----------------------------------- | ---------------------------------------------------- | ------------------------------------------------- |
| 1    | Audit Sprint 1 fixes                | Owner assigned; first ZWCMP call                     | —                                                 |
| 2    | Sprint 1 wraps; pen-test SOW signed | Weekly cadence; DR drill scheduled                   | Pin trust anchor (20-line PR); CI for npm publish |
| 3    | Sprint 2; pen-test kickoff          | DR drill executed; SLA → legal                       | npm publish v1.1; blog draft                      |
| 4    | Sprint 2 wraps; pen-test in flight  | Insurance quote; SLA filled; pilot out for signature | Blog published; 5 dev outreach calls              |

End of week 4: technical debt closed, external review in progress,
ZWCMP signature in motion, primitives published.

---

## Decisions needed from leadership

1. **Pick the motion** — product-led or sales-led for next 12 months. Determines where the second engineer goes.
2. **Name the ZWCMP owner.** That single line in `pilot-success-criteria.md:38` is load-bearing.
3. **Approve pen-test budget envelope** ($40-80k range).
4. **Decide @types/node dependabot pin** (Tier 5 above) — small but blocks dependabot drain.
5. **Accept this audit's findings** as the baseline for the next cycle, OR commission external review BEFORE acting on Sprint 1. The latter is safer but adds 4 weeks of latency.

---

## What did NOT get committed in this session's audit

This document captures conversation output. The audit found 27 issues
but I deliberately did NOT push fixes because:

- The audit's primary finding is that I shouldn't be the one shipping
  security-class fixes without external sign-off
- The Sprint 1 plan is ready to execute; leadership approval +
  ideally a second pair of eyes (even an internal one with no
  prior-session context) before merging
- Some fixes (e.g. F4 platform-tenant tag) involve schema/contract
  decisions that should be ratified, not auto-merged

The fastest path forward is: leadership reviews this doc, ratifies
the Sprint 1 plan + Bet 1 budget, then a different engineer (or
this same agent under different framing) executes Sprint 1 with
explicit per-fix sign-off.

---

## Related artifacts (prior session)

- [`master-audit-2026-05-28.md`](./master-audit-2026-05-28.md) — prior baseline audit
- [`internal-10-10-signoff-2026-05-28.md`](./internal-10-10-signoff-2026-05-28.md) — prior internal sign-off (the 10/10 claim this audit revises)
- [`external-dependencies-register-2026-05-28.md`](./external-dependencies-register-2026-05-28.md) — external blockers (some closed by this session, e.g. EXT-INF-004 staging DNS)
- [`../compliance/verification-sla-draft.md`](../compliance/verification-sla-draft.md) — SLA draft shipped this session (S6.5)
- [`../agents/workflows/dependency-bumps.md`](../agents/workflows/dependency-bumps.md) — dependabot triage policy shipped this session (S5.2)
  </content>
