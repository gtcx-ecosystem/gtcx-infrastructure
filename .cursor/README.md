# `.cursor/` — Cursor IDE agent configuration

Cursor-specific rules, permissions, and CLI settings for agents working in `fabric-os`.

| Path               | Purpose                                                        |
| ------------------ | -------------------------------------------------------------- |
| `rules/*.mdc`      | Protocol 26/27/28, staging execution, status-update discipline |
| `permissions.json` | Tool allow/deny defaults for this workspace                    |
| `cli.json`         | Cursor CLI workspace overrides                                 |

**Owner:** Platform engineering. Do not duplicate normative protocol text here — link to `gtcx-docs` and repo `AGENTS.md`.
