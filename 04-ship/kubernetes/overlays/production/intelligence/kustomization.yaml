# =============================================================================
# Intelligence Production Overlay
# =============================================================================
# Migrated from legacy 04-ship/k8s/intelligence/ directory.
# Contains canary deployment (Argo Rollouts), secrets template,
# and intelligence namespace definition.
# =============================================================================
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - secrets.yaml
  - canary.yaml
  - canary-sdk.yaml
  - ingress.yaml
