# =============================================================================
# terminal-os-staging — ESO SecretStore
# =============================================================================
# Reads from AWS Secrets Manager (af-south-1) via IRSA.
# Prerequisite: IAM role from Terraform module.secrets (terminal-os.tf).
# =============================================================================
apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: terminal-os-aws-secrets
  namespace: terminal-os-staging
  labels:
    app.kubernetes.io/part-of: gtcx
    app.kubernetes.io/component: terminal-os
    environment: staging
spec:
  provider:
    aws:
      service: SecretsManager
      region: af-south-1
      auth:
        jwt:
          serviceAccountRef:
            name: terminal-os-sa
