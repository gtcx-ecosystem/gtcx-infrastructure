apiVersion: v1
kind: Service
metadata:
  name: did-resolver
  namespace: gtcx-staging
  labels:
    app: did-resolver
spec:
  selector:
    app: did-resolver
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
