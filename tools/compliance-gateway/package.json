{
  "name": "@gtcx/compliance-gateway",
  "version": "0.1.0",
  "description": "AI-native compliance query gateway — natural language to protocol handler routing",
  "type": "module",
  "main": "src/server.mjs",
  "scripts": {
    "start": "node src/server.mjs",
    "dev": "node --watch src/server.mjs",
    "test": "node --test tests/**/*.test.mjs",
    "test:coverage": "c8 --reporter=text --reporter=json --reporter=lcov --include='src/**' node --test tests/**/*.test.mjs",
    "_coverage_gate_note": "branches/functions thresholds at 85 (not 90) because audit-sink.mjs soft-loads nats and server.mjs has startup-listener/SIGTERM paths that genuinely require real broker/process integration to exercise. statements + lines remain at 90 since the only excluded branches are the ones above. See docs/audit/coverage-gate-rationale.md.",
    "test:coverage:gate": "c8 --check-coverage --branches 85 --statements 90 --functions 85 --lines 90 --reporter=text --reporter=json --reporter=lcov --include='src/**' node --test tests/**/*.test.mjs"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^1.0.0",
    "@ai-sdk/google": "^1.0.0",
    "@ai-sdk/openai": "^3.0.65",
    "@gtcx/audit-signer": "workspace:*",
    "@gtcx/compliance-data": "workspace:*",
    "ai": "^5.0.52",
    "ioredis": "^5.10.1",
    "zod": "^3.23.0"
  },
  "keywords": [
    "gtcx",
    "compliance",
    "ai-gateway",
    "african-fintech",
    "mcp"
  ],
  "license": "MIT",
  "private": true
}
