{
  "name": "@gtcx/anomaly-detector",
  "version": "0.1.0",
  "description": "Audit-chain anomaly detection CronJob: flags volume drops, off-hours spikes, DID-pattern outliers",
  "type": "module",
  "main": "detector.mjs",
  "scripts": {
    "lint": "eslint . --ext .mjs,.js",
    "test": "node --test tests/*.test.mjs",
    "start": "node detector.mjs",
    "typecheck": "echo \"No TypeScript files \u2014 typecheck is a no-op for this JS-only package\"",
    "build": "echo \"No build step required \u2014 this package runs directly with Node.js ESM\""
  },
  "engines": {
    "node": ">=20.18.0"
  },
  "license": "MIT",
  "private": true
}
